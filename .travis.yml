language: java
cache:
  directories:
    - $HOME/.gradle/caches/

install: echo no installing

script: echo "not testing"

deploy:
    provider: script
    script:
        - build/deploy.sh $TRAVIS_BRANCH
    on:
        tags: false
        all_branches: true
        condition: $TRAVIS_BRANCH =~ ^dev|master$
